<Application x:Class="SSDLauncher_2._0.App"
             ThemeMode="Dark"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SSDLauncher_2._0"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#3B82F6" />
        <SolidColorBrush x:Key="AppBackgroundBrush" Color="#1A1A2E" />
        <Color x:Key="AccentColorValue">#3B82F6</Color>
        <ImageBrush x:Key="AppBackgroundImageBrush" />
        <FontFamily x:Key="AppFontFamily">Segoe UI</FontFamily>
        <Style x:Key="ExecutableRadioStyle" TargetType="RadioButton">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="RowBg" CornerRadius="4" Padding="{TemplateBinding Padding}" Background="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse x:Name="Dot" Width="14" Height="14" Margin="0,0,10,0"
                                 Stroke="{DynamicResource AccentBrush}" StrokeThickness="2" />
                                <ContentPresenter VerticalAlignment="Center" TextElement.Foreground="White" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Dot" Property="Fill" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="RowBg" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RowActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" CornerRadius="4" Padding="{TemplateBinding Padding}"
                        BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="White" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CartridgeButtonStyle" TargetType="Button">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Shell" CornerRadius="10" BorderThickness="3"
                            BorderBrush="{DynamicResource AccentBrush}"
                            Background="{DynamicResource AppBackgroundBrush}">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ShellScale" ScaleX="1" ScaleY="1" />
                            </Border.RenderTransform>
                            <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                            <Border.Effect>
                                <DropShadowEffect x:Name="ShellShadow" Color="Black" Opacity="0.6"
                                              BlurRadius="10" ShadowDepth="4" Direction="270" />
                            </Border.Effect>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="10" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Margin="6,6,6,0" CornerRadius="6" ClipToBounds="True">
                                    <ContentPresenter />
                                </Border>

                                <UniformGrid Grid.Row="1" Rows="1" Columns="6" Margin="16,2">
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                    <Rectangle Fill="{DynamicResource AccentBrush}" Opacity="0.5" Margin="1,0" RadiusX="1" RadiusY="1" />
                                </UniformGrid>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleX" To="1.05" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleY" To="1.05" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="BlurRadius" To="20" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="ShadowDepth" To="8" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="BlurRadius" To="10" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="ShadowDepth" To="4" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Shell" Property="BorderThickness" Value="5" />
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleX" To="1.05" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleY" To="1.05" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="BlurRadius" To="25" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="ShadowDepth" To="0" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="BlurRadius" To="10" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="ShadowDepth" To="4" Duration="0:0:0.15" />
                                            <DoubleAnimation Storyboard.TargetName="ShellShadow" Storyboard.TargetProperty="Opacity" To="0.6" Duration="0:0:0.15" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="14" Background="{DynamicResource AccentBrush}" Opacity="0.85">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextElement.Foreground="White" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
